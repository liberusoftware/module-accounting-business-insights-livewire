<div class="space-y-4"><div class="flex items-center justify-between"><h2 class="text-lg font-semibold">Business insights</h2><input wire:model.live="metric" placeholder="Filter metric" class="rounded border-gray-300" /></div><div class="grid gap-3 md:grid-cols-3">@forelse($insights as $insight)<article class="rounded border p-4"><div class="text-sm text-gray-500">{{ $insight->metric }}</div><div class="text-2xl font-semibold">{{ $insight->value }} {{ $insight->unit }}</div><div class="text-sm">{{ $insight->explanation }}</div><div class="text-xs text-gray-500">{{ $insight->period_start?->toDateString() }} — {{ $insight->period_end?->toDateString() }}</div></article>@empty<p>No insights found.</p>@endforelse</div>{{ $insights->links() }}</div>
